<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DictationIme">

        <activity
            android:name=".auth.AuthGateActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".auth.LoginEmailActivity"
            android:exported="false" />

        <activity
            android:name=".auth.LoginOtpActivity"
            android:exported="false" />

        <activity
            android:name=".auth.OnboardingActivity"
            android:exported="false" />

        <activity
            android:name=".settings.MainActivity"
            android:exported="false" />

        <activity
            android:name=".settings.ProfileActivity"
            android:exported="false" />

        <activity
            android:name=".settings.SettingsActivity"
            android:exported="false" />

        <activity
            android:name=".settings.DataPrivacyActivity"
            android:exported="false" />

        <activity
            android:name=".settings.AppProfilesActivity"
            android:exported="false" />

        <activity
            android:name=".settings.ModelSelectionActivity"
            android:exported="false" />

        <activity
            android:name=".settings.CreditStoreActivity"
            android:exported="false" />

        <activity
            android:name=".settings.TranscriptionDetailActivity"
            android:exported="false" />

        <activity
            android:name=".settings.TransactionHistoryActivity"
            android:exported="false" />

        <service
            android:name=".ime.DictationImeService"
            android:directBootAware="true"
            android:exported="true"
            android:label="@string/ime_name"
            android:permission="android.permission.BIND_INPUT_METHOD">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>

            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>
    </application>

</manifest>
